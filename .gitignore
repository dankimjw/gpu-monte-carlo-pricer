# Build artifacts
build/
*.o

# Binaries
mc_pricer
tests/run_tests

# Output images (keep PNGs for showcase, ignore PPMs)
output/*.ppm

# Local working files (plans, test scripts, scratch)
.local/
opencl/build/
opencl/mc_pricer_ocl
